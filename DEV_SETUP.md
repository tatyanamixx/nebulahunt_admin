# 🚀 Настройка разработки - Nebulahunt Admin Panel

## 📋 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp env.example .env
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 🔧 Режим разработки

### Автоматические моки

В режиме разработки (`VITE_DEV_MODE=true`) приложение использует моки API:

-   ✅ **Статистика дашборда** - тестовые данные
-   ✅ **Список пользователей** - тестовые пользователи
-   ✅ **Приглашения** - тестовые приглашения
-   ✅ **Аутентификация** - упрощенная проверка

### Тестовые данные для входа

```
Email: admin@test.com
Пароль: testpass123
2FA код: любой 6-значный код
```

## 📁 Структура проекта

```
src/
├── pages/                 # Страницы приложения
│   ├── AdminLogin.tsx     # Вход администратора
│   ├── AdminInvite.tsx    # Отправка приглашений
│   ├── AdminRegister.tsx  # Регистрация по приглашению
│   ├── Dashboard.tsx      # Главная панель
│   ├── Users.tsx          # Управление пользователями
│   └── Settings.tsx       # Настройки
├── components/            # Переиспользуемые компоненты
│   ├── Layout.tsx         # Основной макет
│   └── InviteInfo.tsx     # Список приглашений
├── contexts/              # React контексты
│   └── AuthContext.tsx    # Контекст аутентификации
└── lib/                   # Утилиты и конфигурация
    ├── api.ts            # API клиент с моками
    ├── env.ts            # Переменные окружения
    └── utils.ts          # Общие утилиты
```

## 🔐 Система аутентификации

### Google 2FA

-   Все администраторы должны настроить Google Authenticator
-   QR-коды генерируются автоматически
-   Поддерживается ручной ввод секрета

### Email приглашения

-   Приглашения отправляются по email
-   Токены имеют ограниченное время действия (7 дней)
-   Валидация токенов при регистрации

## 🧪 Тестирование

### Доступные страницы

1. `/admin/login` - Вход администратора
2. `/admin/invite` - Отправка приглашений
3. `/admin/register?token=...` - Регистрация по приглашению
4. `/dashboard` - Главная панель (требует аутентификации)
5. `/users` - Управление пользователями
6. `/settings` - Настройки

### Тестовые сценарии

1. **Вход администратора:**

    - Email: `admin@test.com`
    - 2FA: любой 6-значный код

2. **Отправка приглашения:**

    - Заполните форму на `/admin/invite`
    - Выберите роль (ADMIN/SUPERVISOR)

3. **Регистрация по приглашению:**
    - Перейдите по ссылке из email
    - Настройте Google 2FA
    - Завершите регистрацию

## 🛠️ Отладка

### Логи в консоли

В режиме разработки включены подробные логи:

-   Загрузка переменных окружения
-   API запросы и ответы
-   Состояние аутентификации

### Инструменты разработчика

-   **Network** - просмотр API запросов
-   **Console** - логи и ошибки
-   **Application** - токены и localStorage

## ⚠️ Важные моменты

### Безопасность

-   В режиме разработки отключены некоторые проверки безопасности
-   Тестовые данные не должны использоваться в продакшене
-   Все секреты должны быть заменены на реальные

### Производительность

-   Моки API работают синхронно
-   В продакшене используйте реальные API эндпоинты
-   Оптимизируйте запросы к серверу

## 🚀 Развертывание

### Продакшен

1. Отключите режим разработки: `VITE_DEV_MODE=false`
2. Настройте реальные API эндпоинты
3. Настройте SMTP для отправки email
4. Соберите приложение: `npm run build`

### Docker

```bash
# Сборка
docker build -t nebulahunt-admin .

# Запуск
docker run -p 3000:3000 nebulahunt-admin
```

## 📞 Поддержка

### Проблемы

1. **Ошибки сети** - проверьте, что API сервер запущен
2. **Ошибки аутентификации** - проверьте токены в localStorage
3. **Ошибки 2FA** - убедитесь, что код введен правильно

### Логи

Все ошибки логируются в консоль браузера. Проверьте:

-   Network tab для API ошибок
-   Console tab для JavaScript ошибок
-   Application tab для проблем с токенами
