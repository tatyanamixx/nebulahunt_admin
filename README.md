# Nebulahunt Admin Panel

Админ-панель для управления игрой Nebulahunt с аутентификацией через Telegram WebApp.

## Возможности

-   🔐 Аутентификация через Telegram WebApp
-   🔒 Двухфакторная аутентификация (2FA)
-   ⚙️ Настройки игры (экономика, бонусы, параметры)

## Быстрый старт

1. **Установка зависимостей:**

```bash
npm install --legacy-peer-deps
```

2. **Запуск сервера разработки:**

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## Использование

### 1. Вход в систему

1. Откройте приложение в Telegram WebApp
2. Нажмите "Войти через Telegram"
3. Введите код 2FA из приложения аутентификации
4. После успешного входа вы попадете на страницу настроек

### 2. Управление настройками

На странице настроек можно изменить:

-   **Ежедневный бонус Stardust** - количество Stardust за ежедневный вход
-   **Ежедневный бонус Dark Matter** - количество Dark Matter за ежедневный вход
-   **Базовая цена галактики** - стоимость покупки новой галактики
-   **Шанс выпадения артефакта** - вероятность получения артефакта (0.0 - 1.0)
-   **Лимит таблицы лидеров** - максимальное количество игроков в топе

### 3. Сохранение изменений

-   Нажмите "Сохранить" для применения изменений
-   Нажмите "Сбросить" для возврата к значениям по умолчанию

## API Endpoints

### Аутентификация

-   `POST /api/admin/login` - Вход через Telegram
-   `POST /api/admin/2fa/verify` - Проверка 2FA

### Настройки

-   `GET /api/admin/settings` - Получение настроек
-   `PUT /api/admin/settings` - Обновление настроек

## Структура проекта

```
src/
├── contexts/
│   └── AuthContext.tsx    # Контекст аутентификации
├── lib/
│   ├── api.ts            # HTTP клиент
│   └── utils.ts          # Утилиты
├── pages/
│   ├── Login.tsx         # Страница входа
│   └── Settings.tsx      # Страница настроек
├── App.tsx               # Главный компонент
└── main.tsx             # Точка входа
```

## Безопасность

-   Все запросы к API защищены Telegram WebApp аутентификацией
-   Двухфакторная аутентификация для администраторов
-   JWT токены для сессий

## Разработка

### Добавление новых настроек

1. Добавьте поле в интерфейс `GameSettings`
2. Добавьте объект в массив `settingFields`
3. Обновите API эндпоинты на сервере

### Добавление новых страниц

1. Создайте компонент в папке `pages/`
2. Добавьте маршрут в `App.tsx`

## Переменные окружения

Приложение использует переменные окружения для настройки различных аспектов:

```env
# API Configuration
VITE_API_URL=http://localhost:3001/api

# Development Settings
VITE_DEV_MODE=true
VITE_ENABLE_MOCK_API=true

# Feature Flags
VITE_ENABLE_2FA=true
VITE_ENABLE_TELEGRAM_AUTH=true
```

Подробная документация: [Переменные окружения](ENV_VARIABLES_GUIDE.md)

## Технологии

-   **Frontend**: React 18 + TypeScript + Vite
-   **UI**: Tailwind CSS
-   **Роутинг**: React Router DOM
-   **HTTP клиент**: Axios
