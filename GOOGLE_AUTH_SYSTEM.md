# Система Google Auth 2FA и приглашений администраторов

## 📋 Обзор

Система была переписана для поддержки:

-   **Google Authenticator 2FA** - двухфакторная аутентификация через Google Authenticator
-   **Email приглашения** - отправка приглашений администраторам по email
-   **Безопасная регистрация** - регистрация только по приглашениям

## 🔐 Основные компоненты

### 1. Страница приглашений (`/admin/invite`)

-   Отправка приглашений новым администраторам
-   Выбор роли (Администратор/Супервайзер)
-   Валидация email адресов

### 2. Страница регистрации (`/admin/register`)

-   Регистрация по токену приглашения
-   Настройка Google Authenticator
-   Завершение 2FA настройки

### 3. Обновленная страница входа (`/admin/login`)

-   Вход по email + Google 2FA
-   Ссылка на запрос приглашения

## 🚀 API Endpoints

### Приглашения

```typescript
// Отправить приглашение
POST /admin/invite
{
  email: string;
  name: string;
  role: 'ADMIN' | 'SUPERVISOR';
}

// Валидировать токен приглашения
GET /admin/invite/validate?token=string

// Получить список приглашений
GET /admin/invites
```

### Регистрация и 2FA

```typescript
// Регистрация администратора
POST /admin/register
{
  email: string;
  password: string;
  name: string;
  inviteToken: string;
}

// Завершение настройки 2FA
POST /admin/2fa/complete
{
  email: string;
  otp: string;
  inviteToken: string;
}

// Вход администратора
POST /admin/login
{
  email: string;
}

// Верификация 2FA
POST /admin/2fa/verify
{
  email: string;
  otp: string;
}
```

## 🔧 Настройка окружения

### Переменные окружения

```env
# Email Configuration
VITE_SMTP_HOST=smtp.gmail.com
VITE_SMTP_PORT=587
VITE_SMTP_USER=your_email@gmail.com
VITE_SMTP_PASS=your_app_password

# App Configuration
VITE_APP_NAME=Nebulahunt Admin Panel
VITE_APP_URL=http://localhost:3000

# Feature Flags
VITE_ENABLE_2FA=true
VITE_ENABLE_EMAIL_INVITES=true
VITE_ENABLE_GOOGLE_AUTH=true
```

## 📱 Google Authenticator

### Настройка

1. Администратор получает приглашение по email
2. Переходит по ссылке для регистрации
3. Заполняет форму регистрации
4. Получает QR-код для Google Authenticator
5. Сканирует QR-код или вводит секрет вручную
6. Вводит 6-значный код для завершения настройки

### Безопасность

-   Секретный ключ генерируется на сервере
-   QR-код содержит otpauth:// URL
-   Код обновляется каждые 30 секунд
-   Поддерживается ручной ввод секрета

## 📧 Email приглашения

### Процесс отправки

1. Администратор заполняет форму приглашения
2. Система генерирует уникальный токен
3. Отправляется email с ссылкой для регистрации
4. Токен имеет ограниченное время действия (7 дней)

### Шаблон email

```
Тема: Приглашение в Nebulahunt Admin Panel

Здравствуйте, {name}!

Вас пригласили стать {role} в системе Nebulahunt.

Для завершения регистрации перейдите по ссылке:
{registration_url}

Ссылка действительна 7 дней.

С уважением,
Команда Nebulahunt
```

## 🛡️ Безопасность

### Меры безопасности

-   Все приглашения требуют токен
-   Токены имеют ограниченное время действия
-   2FA обязательна для всех администраторов
-   Пароли должны содержать минимум 8 символов
-   Email адреса валидируются

### Роли и права

-   **SUPERVISOR** - полный доступ ко всем функциям
-   **ADMIN** - ограниченный доступ к управлению

## 🧪 Тестирование

### Режим разработки

В режиме разработки (`VITE_DEV_MODE=true`):

-   Автозаполнение тестовых данных
-   Мок API для тестирования
-   Упрощенная валидация

### Тестовые данные

```typescript
// Email
admin@test.com

// Пароль
testpass123

// 2FA код
Любой 6-значный код
```

## 📁 Структура файлов

```
src/
├── pages/
│   ├── AdminInvite.tsx      # Страница приглашений
│   ├── AdminRegister.tsx    # Страница регистрации
│   └── AdminLogin.tsx       # Обновленная страница входа
├── components/
│   └── InviteInfo.tsx       # Компонент списка приглашений
├── contexts/
│   └── AuthContext.tsx      # Обновленный контекст аутентификации
└── lib/
    ├── api.ts              # API конфигурация с моками
    └── env.ts              # Утилиты окружения
```

## 🔄 Миграция с Telegram Auth

### Изменения

1. Удалена зависимость от Telegram WebApp
2. Добавлена поддержка email аутентификации
3. Обновлены переменные окружения
4. Изменена логика проверки окружения

### Совместимость

-   Старые токены Telegram больше не поддерживаются
-   Требуется перерегистрация всех администраторов
-   Новые приглашения отправляются только по email

## 🚀 Развертывание

### Продакшен

1. Настройте SMTP сервер
2. Обновите переменные окружения
3. Отключите режим разработки
4. Отправьте приглашения администраторам

### Docker

```bash
# Сборка
docker build -t nebulahunt-admin .

# Запуск
docker run -p 3000:3000 \
  -e VITE_API_URL=https://api.nebulahunt.com/api \
  -e VITE_SMTP_HOST=smtp.gmail.com \
  -e VITE_SMTP_USER=admin@nebulahunt.com \
  nebulahunt-admin
```

## 📞 Поддержка

### Проблемы

-   Проверьте настройки SMTP
-   Убедитесь в правильности переменных окружения
-   Проверьте логи сервера

### Логирование

Все операции логируются:

-   Отправка приглашений
-   Попытки регистрации
-   Настройка 2FA
-   Попытки входа
